services:
  hawser:
    image: ghcr.io/finsys/hawser:${TAG:-0.2.39}
    container_name: ${CNTNR:-hawser}
    restart: unless-stopped
    ports:
      - ${HOST_PORT:-2376}:2376
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TOKEN=${TOKEN}
